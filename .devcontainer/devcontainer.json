{
  "name": "march-mania-go",
  "image": "mcr.microsoft.com/devcontainers/go:1.22",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y unzip python3-pip && pip3 install --user kaggle && echo 'export PATH=$PATH:$HOME/.local/bin' >> ~/.bashrc"
}
